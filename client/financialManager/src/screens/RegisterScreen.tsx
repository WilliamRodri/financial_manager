import * as React from 'react';
import { Text } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

function RegisterPage() {
    return(
        <SafeAreaView>
            <Text style={{ color: 'black' }}>Register Page</Text>
        </SafeAreaView>
    );
}
export default RegisterPage;